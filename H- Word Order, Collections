# Enter your code here. Read input from STDIN. Print output to STDOUT
from collections import Counter
n=int(input())
arr=[]
d={}
count=0
for _ in range(n):
    arr.append(input())
res=list(Counter(arr).items())
print(len(res))
for i,j in res:
    print(j,end=" ")
# res=Counter(arr).items()
# print(len(res))
# li=list(Counter(arr).values())
# for i in li:
#     print(i,end=" ")


Input:
4
bcdef
abcdefg
bcde
bcdef

Output:
3 1 1

Explanation:
There are  distinct words. Here, "bcdef" appears twice in the input at the first and last positions. The other words appear once each. 
The order of the first appearances are "bcdef", "abcdefg" and "bcde" which corresponds to the output.
