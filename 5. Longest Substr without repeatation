Brute Force Method:
class Solution:
    def lengthOfLongestSubstring(self, s: str) -> int:
        max_count=0
        for i in range(len(s)):
            arr=[]
            count=0
            for j in range(i,len(s)):
                if s[j] in arr:
                    break
                else:
                    arr.append(s[j])
                    count+=1
            if count>max_count:
                max_count=count
        return max_count
    ------------------------------------------------------------------------------------------
    Optimal Soln:
    
