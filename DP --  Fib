Top down approach
def fib(n):
    if n in [1,2]:
        res[n] = 1
        return 1
    if n in res:
        return res[n]
    else:
        res[n] = fib(n-1)+fib(n-2)
        return res[n]

res = {}  
fib(10)
print(list((res.values())))

Bottom Up approach
def tabfib(n,tab=[]):
    tab = [0,1]
    for i in range(2, n+1):
        tab.append(tab[i-1]+tab[i-2])
    return tab
  
tab=tabfib(10)
print(tab)
