if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    arr=list(arr)
    res=set()
    for i in arr:
        res.add(i)
    res=list(res)
    res.sort()
    print(res[-2])
    
    sort()--sorts the value
    set()--sorts and no dup allowed
    to initialize empty set-- empty_set=set()
    We are sorting at the end coz its unordered set and negative number will be present at last
    so to cover up all test cases sort
    
    input
    4
    2 6 5 6
    output
    5
    
    ANOTHER APPROACH
    
    
    if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    arr=list(arr)
    x=max(arr)
    uniq=[]
    for i in arr:
        if i not in uniq:
            uniq.append(i)
    uniq.remove(x)
    print(max(uniq))
    
    
    Traditional Approach
    
    if __name__ == '__main__':
    n = int(input())
    arr = map(int, input().split())
    arr=list(arr)
    x=max(arr)
    res= -101
    for i in arr:
        if res<x and res<=i and i!=x:
            res=i
    print(res)
